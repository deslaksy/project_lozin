A = list(map(int, input("Введите элементы списка через пробел: ").split()))
N = len(A)
# Элементы с нечётными индексами (0, 2, 4, ...)
print("Нечётные позиции:", A[0:N:2])

# Элементы с чётными индексами (1, 3, 5, ...) в обратном порядке
print("Чётные позиции (обратный порядок):", A[1:N:2][::-1])
